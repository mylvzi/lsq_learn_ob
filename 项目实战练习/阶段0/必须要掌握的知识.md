# 一.POJO
>POJO(Plain Old Java Object): 不依赖框架, 表示数据的Java对象

是一种后端开发过程中常用的数据表达语言
一般遵循JavaBean规范: `private field + public method`

# 二.常用注解
## 1.@RestController
> RestController = @Controller + @ResponseBody

作用: 直接把返回值写入到HttpResponseBody, 并调用HTTP服务器进行返回
