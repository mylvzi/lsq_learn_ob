惩罚机制(penalty mechanism)是为了避免生成重复性较高,缺乏创造性内容的一种机制,通过这种机制对大模型进行惩罚,从而实现对于生成内容多样性 的调整

惩罚机制主要有两种:
`频率惩罚(frequency penalty)` 
	依据token在已生成序列中出现的频率作为惩罚力度的判断依据,出现频率越高,惩罚力度越大,抑制模型的啰嗦程度

`存在惩罚(presence penalty)` 
	对所有的token都持有相同力度的惩罚,只要出现过,就惩罚,不考虑出现的频率,没有施加频率的权重

